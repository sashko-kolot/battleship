package application;

import javafx.scene.shape.Rectangle;

class Utils {
	/*
	
	static void mouseClickManager(Grid grid, Rectangle rect, int col, int row, int clickCounter) {
		
		if(grid.isShootingGrid()) {
			shoot(col, row);
		} else {
			if(clickCounter == 1) {
				getPlaceShipDirections();
			}
		}
		
	}
	static void buildShip(int col, int row) {
	
	}

	static void shoot(int col, int row) {
	/*1. Takes cell coordinates.
	 *2. Checks if there is a ship on the opponent's ship grid.
	 *3. If yes, marks the square on oponent's ship grid as hit (setFill Color.RED), marks the square on the player's shooting grid as hit (setFill Color.RED),
	 *4. changes the cell status to hit 
	 * */
	
}
